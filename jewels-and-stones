class Solution {
public:
    int numJewelsInStones(string jewels, string stones) {
        int n=0;
        for(char c: jewels)
        {
            for(char d: stones)
            {
                if(c==d)
                n++;
            }
        }
        return n;
    }
};
